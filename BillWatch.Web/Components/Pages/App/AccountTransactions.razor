@page "/app/account/transactions"

@attribute [Microsoft.AspNetCore.Authorization.Authorize]

@layout AppLayout

@implements IAsyncDisposable

@inject IJSRuntime JS
@inject Microsoft.Extensions.Localization.IStringLocalizer<AccountTransactions> L

<PageTitle>@L["Transactions — BillWatch"]</PageTitle>

<link rel="stylesheet"
      href="/account-transactions.css" />

<div class="app-page">
    <a class="transaction-back-link"
       href="/app/account">
        ← @L["Account"]
    </a>

    <header class="page-heading">
        <div>
            <span class="page-kicker">
                @L["Transaction-first intelligence"]
            </span>

            <h1>
                @L["Transactions"]
            </h1>

            <p>
                @L["The connected-bank activity BillWatch uses to discover recurring bills."]
            </p>
        </div>
    </header>

    @if (_isLoading)
    {
        <div class="transaction-summary-grid">
            <div class="transaction-skeleton"></div>
            <div class="transaction-skeleton"></div>
            <div class="transaction-skeleton"></div>
        </div>

        <div class="transaction-list-skeleton"></div>
    }
    else if (!string.IsNullOrWhiteSpace(
        _errorMessage))
    {
        <div class="app-error-state">
            <div class="state-symbol">
                !
            </div>

            <div>
                <h2>
                    @L["We couldn't load transactions"]
                </h2>

                <p>
                    @_errorMessage
                </p>
            </div>

            <button type="button"
                    class="secondary-action"
                    @onclick="ReloadAsync">
                @L["Try again"]
            </button>
        </div>
    }
    else
    {
        <section class="transaction-summary-grid">
            <article>
                <span>
                    @L["Loaded"]
                </span>

                <strong>
                    @_transactions.Count
                </strong>

                <small>
                    @L["Latest {0} transactions", TransactionLoadLimit]
                </small>
            </article>

            <article>
                <span>
                    @L["Posted outflow"]
                </span>

                <strong>
                    @FormatMoney(
                        PostedOutflow)
                </strong>

                <small>
                    @L["Across the loaded transaction history"]
                </small>
            </article>

            <article>
                <span>
                    @L["Pending"]
                </span>

                <strong>
                    @PendingCount
                </strong>

                <small>
                    @L["Not yet final"]
                </small>
            </article>
        </section>

        <section class="transaction-toolbar">
            <div class="transaction-search">
                <span aria-hidden="true">
                    ⌕
                </span>

                <input type="search"
                       placeholder="@L["Search merchant or transaction"]"
                       autocomplete="off"
                       @bind="_searchText"
                       @bind:event="oninput" />
            </div>

            <select class="transaction-account-filter"
                    @bind="_selectedAccountId">

                <option value="">
                    @L["All accounts"]
                </option>

                @foreach (var account
                    in AvailableAccounts)
                {
                    <option value="@account.Id.ToString("D")">
                        @GetAccountFilterLabel(
                            account)
                    </option>
                }
            </select>
        </section>

        <div class="transaction-result-line">
            <span>
                @_transactionsResultLabel
            </span>

            @if (_transactions.Count == TransactionLoadLimit)
            {
                <small>
                    @L["Showing the newest {0} records", TransactionLoadLimit]
                </small>
            }
        </div>

        @if (_transactions.Count == 0)
        {
            <section class="transaction-empty">
                <div>
                    ↕
                </div>

                <h2>
                    @L["No transactions yet."]
                </h2>

                <p>
                    @L["Transactions appear after an active bank connection completes synchronization."]
                </p>
            </section>
        }
        else if (FilteredTransactions.Count == 0)
        {
            <section class="transaction-empty compact">
                <h2>
                    @L["No matching transactions."]
                </h2>

                <p>
                    @L["Change the account or search filter."]
                </p>
            </section>
        }
        else
        {
            <section class="transaction-list">
                @foreach (var transaction
                    in FilteredTransactions)
                {
                    <article class="transaction-row">
                        <div class="@GetTransactionIconClass(
                                                                        transaction.Amount)">
                            @GetTransactionIconText(
                                transaction.Amount)
                        </div>

                        <div class="transaction-copy">
                            <div class="transaction-title-line">
                                <strong>
                                    @GetTransactionName(
                                        transaction)
                                </strong>

                                @if (transaction.IsPending)
                                {
                                    <span class="pending-pill">
                                        @L["Pending"]
                                    </span>
                                }
                            </div>

                            <span>
                                @transaction.InstitutionName
                                ·
                                @transaction.AccountName

                                @if (!string.IsNullOrWhiteSpace(
                                    transaction.AccountMask))
                                {
                                    <text>
                                        · •••• @transaction.AccountMask
                                    </text>
                                }
                            </span>

                            @if (!string.IsNullOrWhiteSpace(
                                transaction.CategoryPrimary))
                            {
                                <small>
                                    @FormatCategory(
                                        transaction.CategoryPrimary)
                                </small>
                            }
                        </div>

                        <div class="transaction-date">
                            <strong>
                                @transaction.PostedDate
                                    .ToString(
                                        "MMM d",
                                        System.Globalization.CultureInfo.CurrentUICulture)
                            </strong>

                            <span>
                                @transaction.PostedDate.Year
                            </span>
                        </div>

                        <div class="@GetAmountClass(
                                                                        transaction.Amount)">
                            @FormatLedgerAmount(
                                transaction.Amount)
                        </div>
                    </article>
                }
            </section>
        }

        <div class="transaction-explanation">
            @L["Plaid reports purchase/outflow amounts as positive transaction values. BillWatch displays those as money leaving the account and credits as money returning."]
        </div>
    }
</div>

@code {
    private const int TransactionLoadLimit = 50;

    private IJSObjectReference?
        _bffModule;

    private List<BankTransactionViewModel>
        _transactions = [];

    private bool _isLoading =
        true;

    private string? _errorMessage;

    private string _searchText =
        string.Empty;

    private string _selectedAccountId =
        string.Empty;

    private decimal PostedOutflow =>
        _transactions
            .Where(
                transaction =>
                    !transaction.IsPending &&
                    transaction.Amount >
                        0m)
            .Sum(
                transaction =>
                    transaction.Amount);

    private int PendingCount =>
        _transactions.Count(
            transaction =>
                transaction.IsPending);

    private string _transactionsResultLabel =>
        FilteredTransactions.Count == 1
            ? L["1 transaction"]
            : L["{0} transactions", FilteredTransactions.Count];

    private IReadOnlyList<
        TransactionAccountOption>
        AvailableAccounts =>
            _transactions
                .GroupBy(
                    transaction =>
                        transaction.BankAccountId)
                .Select(
                    group =>
                    {
                        var transaction =
                            group.First();

                        return new
                            TransactionAccountOption(
                                transaction.BankAccountId,
                                transaction.InstitutionName,
                                transaction.AccountName,
                                transaction.AccountMask);
                    })
                .OrderBy(
                    account =>
                        account.InstitutionName)
                .ThenBy(
                    account =>
                        account.AccountName)
                .ToList();

    private IReadOnlyList<
        BankTransactionViewModel>
        FilteredTransactions =>
            _transactions
                .Where(
                    MatchesSelectedAccount)
                .Where(
                    MatchesSearch)
                .OrderByDescending(
                    transaction =>
                        transaction.PostedDate)
                .ThenByDescending(
                    transaction =>
                        transaction.Id)
                .ToList();

    protected override async Task
        OnAfterRenderAsync(
            bool firstRender)
    {
        if (!firstRender)
        {
            return;
        }

        await LoadAsync();

        StateHasChanged();
    }

    private async Task ReloadAsync()
    {
        await LoadAsync();

        StateHasChanged();
    }

    private async Task LoadAsync()
    {
        _isLoading =
            true;

        _errorMessage =
            null;

        try
        {
            _bffModule ??=
                await JS.InvokeAsync<
                    IJSObjectReference>(
                    "import",
                    "./js/bff.js");

            var result =
                await _bffModule
                    .InvokeAsync<
                        BankTransactionViewModel[]>(
                        "getBankTransactions",
                        TransactionLoadLimit);

            _transactions =
                result?
                    .ToList()
                ?? [];
        }
        catch (JSException)
        {
            _errorMessage =
                L["BillWatch could not reach the secure API."];
        }
        catch (Exception)
        {
            _errorMessage =
                L["Something went wrong while loading your transactions."];
        }
        finally
        {
            _isLoading =
                false;
        }
    }

    private bool MatchesSelectedAccount(
        BankTransactionViewModel transaction)
    {
        if (string.IsNullOrWhiteSpace(
                _selectedAccountId))
        {
            return true;
        }

        return Guid.TryParse(
                _selectedAccountId,
                out var accountId) &&
            transaction.BankAccountId ==
                accountId;
    }

    private bool MatchesSearch(
        BankTransactionViewModel transaction)
    {
        if (string.IsNullOrWhiteSpace(
                _searchText))
        {
            return true;
        }

        var search =
            _searchText.Trim();

        return Contains(
                transaction.Name,
                search) ||
            Contains(
                transaction.MerchantName,
                search) ||
            Contains(
                transaction.InstitutionName,
                search) ||
            Contains(
                transaction.AccountName,
                search) ||
            Contains(
                transaction.CategoryPrimary,
                search) ||
            Contains(
                transaction.CategoryDetailed,
                search);
    }

    private static bool Contains(
        string? value,
        string search)
    {
        return !string.IsNullOrWhiteSpace(
                value) &&
            value.Contains(
                search,
                StringComparison.OrdinalIgnoreCase);
    }

    private string GetTransactionName(
        BankTransactionViewModel transaction)
    {
        if (!string.IsNullOrWhiteSpace(
                transaction.MerchantName))
        {
            return transaction.MerchantName;
        }

        if (!string.IsNullOrWhiteSpace(
                transaction.Name))
        {
            return transaction.Name;
        }

        return L["Transaction"];
    }

    private static string FormatLedgerAmount(
        decimal amount)
    {
        if (amount > 0m)
        {
            return
                $"-{Math.Abs(amount):C2}";
        }

        if (amount < 0m)
        {
            return
                $"+{Math.Abs(amount):C2}";
        }

        return
            0m.ToString(
                "C2");
    }

    private static string FormatMoney(
        decimal amount)
    {
        return amount.ToString(
            "C2");
    }

    private static string GetAmountClass(
        decimal amount)
    {
        if (amount > 0m)
        {
            return
                "transaction-amount outflow";
        }

        if (amount < 0m)
        {
            return
                "transaction-amount credit";
        }

        return
            "transaction-amount";
    }

    private static string GetTransactionIconClass(
        decimal amount)
    {
        return amount < 0m
            ? "transaction-icon credit"
            : "transaction-icon outflow";
    }

    private static string GetTransactionIconText(
        decimal amount)
    {
        return amount < 0m
            ? "↓"
            : "↑";
    }

    private string FormatCategory(
        string value)
    {
        var display = value
            .Replace(
                "_",
                " ",
                StringComparison.Ordinal)
            .Trim();

        return L[display];
    }

    private static string GetAccountFilterLabel(
        TransactionAccountOption account)
    {
        var mask =
            string.IsNullOrWhiteSpace(
                account.Mask)
                ? string.Empty
                : $" · •••• {account.Mask}";

        return
            $"{account.InstitutionName} · {account.AccountName}{mask}";
    }

    public async ValueTask DisposeAsync()
    {
        if (_bffModule is null)
        {
            return;
        }

        try
        {
            await _bffModule.DisposeAsync();
        }
        catch (JSDisconnectedException)
        {
        }
    }

    private sealed record
        TransactionAccountOption(
            Guid Id,
            string InstitutionName,
            string AccountName,
            string? Mask);

    public sealed class
        BankTransactionViewModel
    {
        public Guid Id { get; set; }

        public Guid BankAccountId
        {
            get;
            set;
        }

        public string InstitutionName
        {
            get;
            set;
        } = string.Empty;

        public string AccountName
        {
            get;
            set;
        } = string.Empty;

        public string? AccountMask
        {
            get;
            set;
        }

        public string Name
        {
            get;
            set;
        } = string.Empty;

        public string? MerchantName
        {
            get;
            set;
        }

        public decimal Amount
        {
            get;
            set;
        }

        public string? IsoCurrencyCode
        {
            get;
            set;
        }

        public DateOnly PostedDate
        {
            get;
            set;
        }

        public DateOnly? AuthorizedDate
        {
            get;
            set;
        }

        public bool IsPending
        {
            get;
            set;
        }

        public string? CategoryPrimary
        {
            get;
            set;
        }

        public string? CategoryDetailed
        {
            get;
            set;
        }
    }
}